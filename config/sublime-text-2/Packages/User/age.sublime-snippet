<snippet>
	<content><![CDATA[
datediff(yy, @gebdate, @calcdate)
  - CASE WHEN (MONTH(@gebdate) > MONTH(@calcdate))
          OR (MONTH(@gebdate) = MONTH(@calcdate)
           AND DAY(@gebdate) > DAY(@calcdate))
         THEN 1 ELSE 0 END
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>age</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.sql</scope>
</snippet>
